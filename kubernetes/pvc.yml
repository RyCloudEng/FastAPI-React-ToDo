apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ebs
  namespace: production
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: pv-ebs
  resources:
    requests:
      storage: 1Gi
